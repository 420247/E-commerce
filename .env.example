# PostgreSQL — used by docker-compose for local development
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Anthropic Claude API key — get it from console.anthropic.com
ANTHROPIC_API_KEY=

# JWT signing key — must be a Base64-encoded string of at least 256 bits.
JWT_SECRET=